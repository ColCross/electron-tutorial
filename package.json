{
  "name": "tutorialapp",
  "version": "1.0.0",
  "description": "simple electron tutorial app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=app/images/electron.icns --prune=true --out=release-builds"
  },
  "author": "Colten Cross",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.7"
  },
  "devDependencies": {
    "electron-packager": "^12.1.0"
  }
}
